extends layout/layout

block content
	form(action="/resetPassword", method="POST")
		input(type="hidden", name="accountId", value="#{locals.accountId}")
		p
			label(for="password") 新的密码：
			input#password(type="password",name="password")
		p
			input(type="submit")
